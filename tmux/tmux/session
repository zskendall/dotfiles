#!/bin/bash

source ~/dotfiles/tmux/tmux/session_fns

SESSION=""
if [ -r ~/.tmux_work ];
  then SESSION="";
  # then SESSION="work";
fi
MAIN_ON_RIGHT=$1

# Either attach to an existing session or create a new one
tmux a -dt $SESSION || create_session $SESSION $MAIN_ON_RIGHT
